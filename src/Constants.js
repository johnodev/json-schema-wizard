const Constants = {
    nodeTypes: {
        STRING: 'string',
        ARRAY: 'array',
        BOOLEAN: 'boolean',
        INTEGER: 'integer',
        NUMBER: 'number',        
    }
}

export default Constants;
